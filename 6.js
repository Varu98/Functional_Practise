const arr = [1, 2, 55, 22, 99, 44, 33];

// const isEven = (num) => num % 2 === 0;

// const isOdd = (num) => num % 2;

// For Even numbers using Filter Method
// const output = arr.filter(isEven);
// console.log(output);

// For Odd Numbers using Filter Method
// const output = arr.filter(isOdd);
// console.log(output);

// Find Sum using Reduce Method
// const toAdd = arr.reduce((accum, curr) => {
//   accum = accum + curr;
//   return accum;
// }, 0);

// console.log(toAdd);

// Find Max using Reduce Method
// const output = arr.reduce((accum, curr) => {
//   accum < curr ? (accum = curr) : accum;

//   return accum;
// }, 0);

// console.log(output);
